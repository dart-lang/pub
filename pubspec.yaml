name: pub

dependencies:
  # Note: Pub's test infrastructure assumes that any dependencies used in tests
  # will be hosted dependencies.
  analyzer: ^0.32.0
  args: ^1.4.1
  async: ^2.0.0
  collection: "^1.8.0"
  crypto: ">=1.0.0 <3.0.0"
  http: "^0.11.0"
  http_multi_server: ">=1.0.0 <3.0.0"
  http_retry: "^0.1.1"
  http_throttle: "^1.0.0"
  meta: "^1.1.0"
  oauth2: "^1.0.0"
  package_config: "^1.0.0"
  package_resolver: "^1.0.0"
  path: "^1.2.0"
  pool: "^1.0.0"
  pub_semver: "^1.4.0"
  shelf: ^0.7.0
  source_span: "^1.4.0"
  stack_trace: "^1.0.0"
  yaml: "^2.0.0"

dev_dependencies:
  shelf_test_handler: "^1.0.0"
  test: ^1.2.0
  test_descriptor: "^1.0.0"
  test_process: "^1.0.0"
  build_runner: ^0.9.0
  build_modules: ^0.3.0
  build_test: ^0.10.0
  build_vm_compilers: ^0.1.0

dependency_overrides:
  build_modules:
    path: ../build/build_modules
    # git:
    #   url: https://github.com/dart-lang/build.git
    #   ref: run-precompiled-vm-tests
    #   path: build_modules
  build_vm_compilers:
    path: ../build/build_vm_compilers
    # git:
    #   url: https://github.com/dart-lang/build.git
    #   ref: run-precompiled-vm-tests
    #   path: build_vm_compilers
  build_runner:
    path: ../build/build_runner
  build_runner_core:
    path: ../build/build_runner_core

environment:
  sdk: ">=2.0.0-dev.61.0 <2.0.0"
