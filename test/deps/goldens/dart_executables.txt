'-- myapp
    |-- bin
    |   |-- bar.dart
    |   '-- foo.dart
    '-- pubspec.yaml

$ pub deps --executables
myapp: bar, foo

$ pub deps --executables --dev
myapp: bar, foo

$ pub deps --executables --json
{
  "packages": [
    {
      "name": "myapp",
      "executables": [
        "bar",
        "foo"
      ]
    }
  ]
}

$ pub deps --executables --dev --json
{
  "packages": [
    {
      "name": "myapp",
      "executables": [
        "bar",
        "foo"
      ]
    }
  ]
}

